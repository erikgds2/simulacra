# URL do backend — desenvolvimento local
VITE_API_URL=http://localhost:8000

# URL do backend — produção (Render)
# VITE_API_URL=https://simulacra-api.onrender.com

# OPCIONAL — Autenticação Supabase (deixe em branco para desabilitar auth)
# Obtenha em: Supabase → Project Settings → API
# Sem estas vars, o projeto funciona sem autenticação (modo open source)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
